{
  "name": "lootcheck-firefox",
  "version": "0.1.0",
  "description": "Browser extension that shows a quiet ownership bar when you already own a game on the current page.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "assets": "node scripts/generate-assets.mjs",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "fake-indexeddb": "^6.0.0",
    "happy-dom": "^20.10.6",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
